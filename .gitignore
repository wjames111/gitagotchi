# macOS
.DS_Store

# test fixtures are generated by gh-pet/tests/make_fixtures.sh with
# relative dates — regenerated on every run, never source
gh-pet/tests/fixtures/
gh-pet/tests/fixtures-starving/

# derive-layer scratch (written next to the cache when jq fails)
*.state.err
*.state.tmp
